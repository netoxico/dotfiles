Installation:

	git clone git://github.com/netoxico/dotvim.git ~/.vim

Vim plugins installed as git submodules. Check these

	git submodule init
	git submodule update

Create symlinks:

	ln -s ~/.vim/vimrc ~/.vimrc
	ln -s ~/.vim/gvimrc ~/.gvimrc

